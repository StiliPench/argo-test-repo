apiVersion: v1
kind: Service
metadata:
  name: {{ include "rolling-helm.fullname" . }}
  labels:
    {{- include "rolling-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rolling-helm.selectorLabels" . | nindent 4 }}
